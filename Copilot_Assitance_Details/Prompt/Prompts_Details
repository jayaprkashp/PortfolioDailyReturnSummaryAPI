The below prompts are used in Githut Copilot:

1. How to add custom port in application.yaml

2. Generate getters and setters for PortfolioRequest and PortfolioResponse

3. Open PortfolioController.java and add javadoc comments - reverted as the comments made the code tightly packed

4. How to compare if a BigDecimal is greater than 0 or not

5. Generate code to check if the netCashflow is greater than 20% of beginMarketvalue

6. Create sample json input for PortfolioRequest

7. Remove the validationexception and global exception handler and all its references from the code.

8. Generate json input using PortfolioRequest with values for INVALID_INPUT and VALID status

9. Generate comments for PorfolioServiceImpl - reverted as the comments made the code tightly packed and there was a change in implementation

10. Generate a test suite for this repository
